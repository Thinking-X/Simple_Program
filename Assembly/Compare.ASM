DATA SEGMENT
A	DB	06H
B	DB	08H
DATA ENDS
CODE SEGMENT
	ASSUME DS: DATA, CS: CODE
START: MOV AX, DATA
       MOV DS, AX
       MOV AL, A
       CMP AL, B
       JAE NEXT
       MOV AL, B
NEXT: MOV DL, AL
      ADD DL, 30H
      MOV AH, 02H
      INT 21H
      MOV AH, 4CH
      INT 21H
CODE ENDS
END START