DATA SEGMENT
STR	DB	'I AM XINSILU'
DATA ENDS

CODE SEGMENT
	ASSUME DS: DATA, CS: CODE

START:  MOV AX, DATA
	MOV DS, AX

	MOV CX, 12
	MOV DL, 0
	MOV BX, OFFSET STR

 LP:			;(15ÐÐ)
	MOV AL, [BX]
	CMP AL, ' '
	JNZ  LL
	MOV AL, '*'
	MOV [BX],  AL

 LL:	INC BX
	LOOP LP
	
	MOV CX, 12
	LEA BX, STR

 LP2:	MOV AL, [BX]
	MOV DL, AL
	MOV AH, 02H
	INT 21H
	INC BX
	LOOP LP2

	MOV AH, 4CH
	INT 21H
CODE ENDS
END START 