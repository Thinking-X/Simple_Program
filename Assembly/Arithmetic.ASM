;º∆À„X*X+Y-8

DATA	SEGMENT 
X	DB	03H
Y	DB	04H
SUM	DW	?
DATA	ENDS

CODE	SEGMENT
	ASSUME	DS:DATA, CS:CODE
START:	MOV AX, DATA
	MOV DS, AX
	MOV AL, X
	MUL AL
	ADD AX, WORD PTR Y
	SUB AX, 8
	MOV SUM, AX

	ADD AL, 30H
	MOV DL, AL
	MOV AH, 02H
	INT 21H
	
	MOV AH, 4CH
	INT 21H	
CODE	ENDS
	END START