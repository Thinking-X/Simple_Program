;计算数组ARY各元素的累加和，并存于SUM单元中，设累加和不超过8位二进制数
DATA SEGMENT
ARY	DB	5,12,3,6,7,23,0,50,3,1
SUM	DB	?
DATA ENDS

CODE SEGMENT
	ASSUME DS: DATA, CS:CODE
START:  MOV AX, DATA
	MOV DS, AX
	
	MOV DX, 0	
	MOV CX, 10
	LEA BX, ARY

 LP:	MOV AL, [BX]
	ADD DL, AL
	INC BX
	LOOP LP
	
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START