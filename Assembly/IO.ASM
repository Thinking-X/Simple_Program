;
STACK    SEGMENT  PARA  STACK  'STACK'
              DB        100   DUP(?)
STACK     ENDS

CODE SEGMENT
	ASSUME SS:STACK, CS:CODE
START:  MOV CX, 10
 LP1:	MOV AH, 01H	
	INT 21H
	PUSH AX
	LOOP LP1

	MOV CX, 10

 LP2:	POP DX
	MOV AH, 02H
	INT 21H
	LOOP LP2
	
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START