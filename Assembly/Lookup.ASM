DATA SEGMENT
STR DB 'AB C DEF G'
NUM DB ?
DATA ENDS

CODE SEGMENT
	ASSUME DS:DATA, CS:CODE
START:  MOV AX, DATA
	MOV DS, AX
	MOV BX, OFFSET STR
	MOV DL, 0
	MOV CX, 10
 LP:	MOV AL, [BX]
	CMP AL, ' '
	JNZ NEXT
	INC DL
 NEXT:	INC BX
	LOOP LP
	MOV NUM, DL
	
	ADD NUM, 30H
	MOV DL, NUM
	MOV AH, 02H
	INT 21H
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START