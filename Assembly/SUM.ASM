;求以BUF为首地址的10个内存单元的数据和，已知其和小于255，将结果存入RES单元
DATA SEGMENT
BUF	DB	1,2,9,5,-34,8,-10,30,6,-8
RES	DB	?
DATA ENDS

CODE SEGMENT
	ASSUME DS: DATA, CS:CODE
START:  MOV AX, DATA
	MOV DS, AX
	
	MOV AL, 0
	MOV RES, AL	
	MOV CX, 10
	LEA BX, BUF

 LP:	MOV AL, [BX]
	ADD RES, AL
	INC BX
	LOOP LP
	
	MOV DL, RES
	ADD DL, 30H 
	MOV AH, 02H
	INT 21H
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START