DATA SEGMENT
X	DB	06H
Y	DB	?
DATA ENDS

CODE SEGMENT
 	ASSUME DS: DATA, CS: CODE
START:  MOV AX, DATA
	MOV DS, AX
	MOV AL, X
	AND AL, AL
	JS LP1
	JZ LP2
	MOV Y, 1
	JMP DONE
LP1:	MOV Y, -1
	JMP DONE
LP2:	MOV Y, 0
DONE:	ADD Y, 30H
	MOV DL, Y
	MOV AH, 02H
	INT 21H
	MOV AH, 4CH
	INT 21H
CODE ENDS
END START
	